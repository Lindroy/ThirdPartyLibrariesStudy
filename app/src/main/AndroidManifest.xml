<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lindroid.thirdpartylibrariesstudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.CatalogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AnimShopButtonActivity" />
        <activity android:name=".activity.PasscodeViewActivity" />
        <activity android:name=".activity.AutoSystemBarActivity" />
        <activity android:name=".activity.CountdownViewActivity" />
        <activity android:name=".activity.checkversionlib.CheckVersionActivity" />
        <activity
            android:name=".activity.checkversionlib.CheckVersionDialogActivity"
            android:launchMode="singleTask"
            android:theme="@style/versionCheckLibvtransparentTheme" />
        <activity
            android:name=".activity.simplebehavior.SimpleBehaviorActivity"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.simplebehavior.EleShopDetailActivity"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.materialedittext.MaterialEditTextActivity"></activity>
    </application>

</manifest>